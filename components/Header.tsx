export const Header = () => {
  return (
    <header className="bg-black text-white text-center flex items-center justify-center"
      style={{ height: 'var(--header-height)' }}>
      <div>HEADER</div>
    </header>
  );
}